# ==== Microsoft Entra ID (Azure AD) ====
# Your tenant (directory) ID, e.g. 11111111-2222-3333-4444-555555555555
ENTRA_TENANT_ID=YOUR_TENANT_ID
# App registration's Application (client) ID
ENTRA_CLIENT_ID=YOUR_CLIENT_ID
# Client secret (create in App registrations > Certificates & secrets)
ENTRA_CLIENT_SECRET=YOUR_CLIENT_SECRET

# The URL where MS should redirect after login; for local testing leave as-is
OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback

# Session secret for cookie signing (change this!)
SESSION_SECRET=please-change-me

# DB connection string (compose default points to the 'db' service)
DATABASE_URL=postgresql+psycopg2://vertrieb:vertrieb@db:5432/vertrieb

# Path to the customers CSV inside the container
CUSTOMERS_CSV_PATH=/app/data/customers.csv

# Default locale: de or en
DEFAULT_LOCALE=de
